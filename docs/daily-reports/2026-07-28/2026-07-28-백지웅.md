# Daily Report

- 날짜: 2026-07-28
- 작성자: 백지웅
- 관련 Jira 작업: S15P11C103-122

## 오늘 한 일

- Raspberry Pi 5에 Ubuntu Server 24.04 LTS 64-bit를 설치하고 초기 시스템 설정을 진행함.
- Raspberry Pi에 Docker Engine과 Docker Compose 실행 환경을 구성함.
- Ubuntu 22.04 Jammy 및 ROS 2 Humble 기반의 Raspberry Pi용 Docker 이미지와 Compose 설정을 구성함.
- 프로젝트 저장소의 `thing_ws`를 컨테이너의 `/thing_ws`에 bind mount하도록 경로를 설정함.
- Dynamixel 통신을 위해 호스트의 `/dev/ttyUSB0` 장치를 컨테이너에 연결하도록 설정함.
- CycloneDDS, `ROS_DOMAIN_ID=103` 및 host network 기반의 ROS 2 통신 환경을 구성함.
- `colcon build` 과정에서 발생한 `ament_cmake` 탐색 오류를 확인하고 ROS 환경 source 및 빌드 의존성 설치를 통해 해결함.
- 새 컨테이너 셸에서 ROS 2와 워크스페이스 환경이 자동 적용되도록 `/root/.bashrc` 설정을 Docker 이미지에 추가함.
- Ubuntu 설치부터 Docker 이미지 빌드와 ROS 2 통신 확인까지의 Raspberry Pi 환경 구성 절차를 문서화함.

## 결과 및 확인 자료

- 커밋/MR: 작성 예정
- 사진·영상·측정값:
  - Raspberry Pi 5에서 Docker 컨테이너 생성 및 실행 확인
  - ROS 2 Humble 워크스페이스 `colcon build` 정상 완료
  - Docker 설정 경로: `/home/rpi103/rpi-ros2-humble`
  - ROS 2 워크스페이스 경로: `/home/rpi103/thing_pjt/thing_ws`

## 막힌 점 또는 위험 요소

- 현재 Compose 설정이 `/dev/ttyUSB0`에 의존하므로 장치 연결 순서에 따라 이름이 변경될 수 있음. 추후 USB 장치 ID를 기준으로 대상 장치를 식별하여 컨테이너에 연결하도록 개선이 필요함.
- `cyclonedds.xml`에 등록된 네트워크 인터페이스와 피어 IP가 실제 장비의 네트워크 구성과 일치하는지 검증이 필요함.
- 컨테이너 재생성 후에도 빌드 의존성과 ROS 환경 설정이 유지되는지 최종 확인이 필요함.

## 다음 작업

- 새 Docker 이미지를 다시 빌드하고 컨테이너를 재생성하여 ROS 환경 자동 source 여부를 확인함.
- 컨테이너에서 Dynamixel 장치 인식 및 SDK 기반 제어 동작을 검증함.
- 다른 ROS 2 장비와 CycloneDDS 기반 토픽 송수신을 테스트함.
- `/dev/serial/by-id` 또는 udev 속성에서 USB 장치 ID를 조회하고, 해당 ID와 일치하는 Dynamixel 장치를 컨테이너에 연결하도록 Compose 실행 방식을 개선함.
