# Daily Report

- 날짜: 2026-07-28
- 작성자: 김기현
- 관련 Jira 작업: S15P11C103-120

## 오늘 한 일

- **`thing_database_web` 신규 프로젝트 구축**
  - 로봇(라즈베리파이) → EC2 업로드, 회원 → 다운로드 두 가지 신규 요구사항을 구현하는 독립 Django+Vue 프로젝트 생성 (`thing_control_web`과 코드/DB/배포 서버 완전 분리)
  - 백엔드(Django + DRF): `models.py`(`MotorLogFile`, `UserProfile`), `services.py`(`S3Service` — 현재는 로컬 `media/` 폴더를 S3 대역으로 사용하는 스텁 구현, 추후 `boto3` 연동 전환 가능하도록 설계), `views.py`(회원가입/로그인/업로드/다운로드/목록 API) 작성
  - API 설계 및 구현: `/api/register/`, `/api/login/`, `/api/motor-data/upload/`, `/api/motor-data/files/`, `/api/motor-data/download/<id>/`
  - 프런트엔드(Vue 3 + Vite): `HomeView.vue`, `LoginView.vue`, `RegisterView.vue`, `DataDownloadView.vue`, 라우터(`/download` 인증 가드), `services/api.js`/`auth.js` 구현
  - EC2 배포를 전제로 `gunicorn`, S3 설정 뼈대(`AWS_ACCESS_KEY_ID` 등 `.env` 기반) 준비
  - 프로젝트 구조/설계 근거/실행 방법을 README.md 3절에 문서화
- **진행 상태 검증 및 배포 전 항목 일부 수정**
  - [thing_control_web] WS 상태 필드 네이밍 불일치 해소: `HandSocketContext.jsx`, `SafetyBanner.jsx`, `StatusBar.jsx`, `MotorStatusPanel.jsx`, `VisionMode.jsx`, `OrderMode.jsx`의 camelCase 필드(`commandTimeout`, `ownerAlive`, `sequenceRunning`, `busCommunicationOk` 등)를 백엔드(`consumers.py`) 기준 snake_case로 통일

## 결과 및 확인 자료

- 커밋/MR: 해당 없음
- 사진·영상·측정값: 해당 없음 (신규 프로젝트 코드/문서 및 수정 사항, README.md에 구조·API·검증 결과 문서화 / 프런트-백엔드 필드명 grep 검증 결과 camelCase 잔존 없음 확인)

## 막힌 점 또는 위험 요소

- 없음

## 다음 작업

- [thing_database_web] 업로드 API 인증/서명 추가 (`MotorDataUploadView` 등, `permission_classes` 부재로 현재 누구나 호출 가능)
- [thing_database_web] `auth.js`의 `localStorage.isLoggedIn` 기반 로그인 판단을 서버 세션 검증 방식으로 교체
- [thing_database_web] 실제 S3 연동 전환 (`S3Service`의 `upload_file`/`generate_download_url`을 `boto3` 기반으로 교체)
- 실제 배포 환경(EC2)에서 `backend/config/.env` 생성 및 `CORS_ALLOWED_ORIGINS`, `ALLOWED_HOSTS` 등 설정값 확인